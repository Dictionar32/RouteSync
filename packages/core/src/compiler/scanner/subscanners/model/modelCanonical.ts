import type { ModelAst } from '../../../../types/upstream/ast';
import type { ModelDefinition, ModelKeyKind, RelationKind } from '../../../../types/upstream/model';
import type { CastType } from '../../../../types/upstream/expression';
import type { ParsedModel } from '../../../../types/domain/models';
import type { ParsedColumn } from '../../../../types/domain/databaseColumns';
import type { Sequence, PropertyNames } from '../../../../types/upstream/collections';
import type { SourceSpan } from '../../../../types/upstream/provenance';
import type { PrimitiveVocabulary, Cardinality, Nullability } from '../../../../types/upstream/primitiveVocabulary';
import type { TypeExpression } from '../../../../types/upstream/typeVocabulary';
import type { DatabaseType } from '../../../../types/upstream/databaseVocabulary';
import type { ModelFacts } from '../../../../types/upstream/modelSourceFacts';
import type { TokenDescriptor } from '../../LaravelSourceLexer';
import type { ParsedCast, ParsedRelation } from '../../../../types/domain/eloquentTypes';
import type { NumberValue, StringValue, TruthValue } from '../../../../types/upstream/valueObjects';
import { PrimitiveKind, type SemanticType, type SemanticTypeKind } from '../../../types/SemanticType';
import type { TypeProperty, TypeReference, TypeParameters } from '../../../../types/upstream/typeVocabulary';
import { parseModelSourceFacts } from './modelSourceFactsParser';
import { accessorExpression } from './modelAccessorCanonical';

const str = (value: string): StringValue => ({ kind: 'string_value', value });
const num = (value: number): NumberValue => ({ kind: 'number_value', value });
const truth = (value: boolean): TruthValue => ({ kind: 'truth_value', value });
const sequence = <T>(items: readonly T[]): Sequence<T> => items.reduceRight<Sequence<T>>((tail, head) => ({ kind: 'cons', head, tail }), { kind: 'empty' });
const sourceSpan = (file: string, length: number): SourceSpan => ({ kind: 'source_span', file: { kind: 'source_file', value: str(file) }, start: num(0), end: num(length) });
const name = (value: string) => ({ kind: 'model_name', value: str(value) } as const);
const property = (value: string) => ({ kind: 'property_name', value: str(value) } as const);
const column = (value: string) => ({ kind: 'column_name', value: str(value) } as const);
const table = (value: string) => ({ kind: 'table_name', value: str(value) } as const);
const primitive = (value: PrimitiveVocabulary): TypeExpression => ({ kind: 'primitive', value });
import { semanticType } from './semanticTypeCanonical';
const primitiveKind = (kind: string): PrimitiveVocabulary => ({ kind: ({ string: 'string', number: 'number', boolean: 'boolean', datetime: 'date_time', file: 'file', json_value: 'json' } as const)[kind as 'string' | 'number' | 'boolean' | 'datetime' | 'file' | 'json_value'] });
const nullabilityKinds: ReadonlyMap<string, Nullability> = new Map([['nullable', { kind: 'nullable' }], ['non_nullable', { kind: 'non_null' }]]);
const nullability = (kind: string): Nullability => nullabilityKinds.get(kind) || { kind: 'non_null' };

const keyKinds: { readonly [K in ParsedModel['keyType']]: ModelKeyKind } = { int: { kind: 'integer' }, bigint: { kind: 'big_integer' }, string: { kind: 'string' }, uuid: { kind: 'uuid' }, ulid: { kind: 'ulid' } };

const castKinds: { readonly [K in ParsedCast['castKind']]: CastType } = { integer: { kind: 'integer' }, float: { kind: 'float' }, decimal: { kind: 'float' }, boolean: { kind: 'boolean' }, string: { kind: 'string' }, datetime: { kind: 'date_time' }, date: { kind: 'date_time' }, timestamp: { kind: 'date_time' }, array: { kind: 'array' }, json: { kind: 'json' }, object: { kind: 'json' }, collection: { kind: 'array' }, encrypted: { kind: 'string' }, custom: { kind: 'string' } };
const relationKinds: { readonly [K in ParsedRelation['type']]: RelationKind } = { hasOne: { kind: 'has_one' }, hasMany: { kind: 'has_many' }, belongsTo: { kind: 'belongs_to' }, belongsToMany: { kind: 'belongs_to_many' }, hasOneThrough: { kind: 'has_one_through' }, hasManyThrough: { kind: 'has_many_through' }, morphTo: { kind: 'morph_to' }, morphOne: { kind: 'morph_one' }, morphMany: { kind: 'morph_many' }, morphToMany: { kind: 'morph_to_many' }, morphedByMany: { kind: 'morphed_by_many' } };
const castType = (cast: ParsedCast): CastType => castKinds[cast.castKind];
const castExpressionType = (cast: ParsedCast): TypeExpression => primitive({ kind: ({ integer: 'number', float: 'number', decimal: 'number', boolean: 'boolean', string: 'string', datetime: 'date_time', date: 'date_time', timestamp: 'date_time', array: 'json', json: 'json', object: 'json', collection: 'json', encrypted: 'string', custom: 'string' } as const)[cast.castKind] });
const relationCardinalities: { readonly many: Cardinality; readonly one: Cardinality } = { many: { kind: 'many' }, one: { kind: 'one' } };
const relationCardinality = (value: ParsedRelation['cardinality']): Cardinality => relationCardinalities[value];
const relationKey = (relation: ParsedRelation) => 'column' in relation.foreignKey ? { kind: 'explicit' as const, foreign: column(relation.foreignKey.column.value), local: column('id') } : { kind: 'convention' as const };
const databaseKinds: { readonly [K in ParsedColumn['type']['kind']]: DatabaseType } = { bigint: { kind: 'integer', width: { kind: 'big' }, signed: truth(true) }, integer: { kind: 'integer', width: { kind: 'normal' }, signed: truth(true) }, smallint: { kind: 'integer', width: { kind: 'small' }, signed: truth(true) }, tinyint: { kind: 'integer', width: { kind: 'tiny' }, signed: truth(true) }, float: { kind: 'decimal', precision: num(0), scale: num(0) }, double: { kind: 'decimal', precision: num(0), scale: num(0) }, decimal: { kind: 'decimal', precision: num(0), scale: num(0) }, boolean: { kind: 'boolean' }, string: { kind: 'string', length: num(0) }, text: { kind: 'text' }, mediumtext: { kind: 'text' }, longtext: { kind: 'text' }, date: { kind: 'date_time' }, datetime: { kind: 'date_time' }, timestamp: { kind: 'date_time' }, time: { kind: 'date_time' }, json: { kind: 'json' }, enum: { kind: 'enum', values: { kind: 'string_values', items: sequence([]) } }, binary: { kind: 'string', length: num(0) }, uuid: { kind: 'string', length: num(0) }, ulid: { kind: 'string', length: num(0) }, unsupported: { kind: 'string', length: num(0) } };
const properties = (model: ParsedModel, span: SourceSpan) => model.columns.map((item: ParsedColumn) => ({ kind: 'property' as const, name: property(item.propertyName), value: { kind: 'typed' as const, type: semanticType(item.semanticType) }, presence: { kind: 'required' as const }, origin: { kind: 'column' as const, column: column(item.name) }, source: span }));
const exposure = (values: readonly { readonly value: string }[]): PropertyNames => ({ kind: 'property_names', items: sequence(values.map(value => property(value.value))) });

export function modelAstFromParsed(model: ParsedModel, file: string, length: number, tokens: readonly TokenDescriptor[]): ModelAst {
    const span = sourceSpan(file, length);
    const modelProperties = properties(model, span);
    const factsSource = parseModelSourceFacts(tokens, span);
    const definition: ModelDefinition = { kind: 'model', identity: { kind: 'model_identity', name: name(model.name.value), shortName: name(model.shortName.value), table: table(model.table.value), inheritance: factsSource.inheritance }, source: span, file: { kind: 'source_file', value: str(file) }, key: { kind: 'primary_key', column: column(model.primaryKey.value), type: keyKinds[model.keyType], semanticType: primitive({ kind: ({ number: 'number', string: 'string' } as const)[model.keySemanticType.kind] }), autoGenerated: truth(model.incrementing) }, behavior: { kind: 'model_behavior', incrementing: truth(model.incrementing), softDeletes: truth(model.softDeletes), timestamps: truth(model.timestamps) }, exposure: { kind: 'model_exposure', fillable: exposure(model.fillable), guarded: exposure(model.guarded), hidden: exposure(model.hidden), appends: exposure(model.appends) }, capabilities: { kind: 'model_source_capabilities', traits: factsSource.traits }, surface: { kind: 'property_surface', properties: { kind: 'properties', items: sequence(modelProperties) } }, schema: { kind: 'model_schema', columns: { kind: 'columns', items: sequence(model.columns.map((item: ParsedColumn) => ({ kind: 'column' as const, name: column(item.name), databaseType: databaseKinds[item.type.kind], semanticType: primitiveKind(item.semanticType.kind), presence: { kind: 'required' as const }, nullability: nullability(item.nullability.kind), default: { kind: 'none' as const }, primary: truth(Object.is(item.name, model.primaryKey.value)), autoGenerated: truth(Object.is(item.name, model.primaryKey.value) && model.incrementing), source: span }))) }, foreignKeys: { kind: 'foreign_keys', items: sequence([]) }, indexes: { kind: 'indexes', items: sequence([]) } }, properties: { kind: 'properties', items: sequence(modelProperties) }, relations: { kind: 'model_relations', items: sequence(model.relations.map((relation: ParsedRelation) => ({ kind: 'model_relation' as const, name: { kind: 'relation_name' as const, value: str(relation.name.value) }, target: { kind: 'model_name' as const, value: str(relation.targetModel.value) }, relation: relationKinds[relation.type], cardinality: relationCardinality(relation.cardinality), key: relationKey(relation), source: span }))) }, casts: { kind: 'model_casts', items: sequence(model.casts.map((cast: ParsedCast) => ({ kind: 'model_cast' as const, property: { kind: 'property_name' as const, value: str(cast.column.value) }, target: castType(cast), source: span }))) }, computed: { kind: 'model_accessors', items: sequence(model.accessors.map(accessor => ({ kind: 'model_accessor' as const, name: property(accessor.propertyName.value), expression: accessorExpression(accessor.computation, span), source: span }))) }, constants: { kind: 'model_constants', items: sequence(factsSource.constants.map(item => ({ kind: 'model_constant' as const, name: item.name, value: item.value, source: item.source }))) }, methods: { kind: 'model_methods', items: sequence(factsSource.methods.map(item => ({ kind: 'model_method' as const, name: item.name, result: item.result, body: item.body, source: item.source }))) } };
    const facts: ModelFacts = { kind: 'model_facts', identity: { kind: 'model_identity_facts', name: name(model.name.value), shortName: name(model.shortName.value), table: table(model.table.value), inheritance: factsSource.inheritance, source: span }, key: { kind: 'model_key_facts', column: column(model.primaryKey.value), type: keyKinds[model.keyType], semanticType: primitive({ kind: ({ number: 'number', string: 'string' } as const)[model.keySemanticType.kind] }), autoGenerated: truth(model.incrementing) }, behavior: { kind: 'model_behavior_facts', incrementing: truth(model.incrementing), softDeletes: truth(model.softDeletes), timestamps: truth(model.timestamps) }, exposure: { kind: 'model_exposure_facts', fillable: sequence(model.fillable.map(value => property(value.value))), guarded: sequence(model.guarded.map(value => property(value.value))), hidden: sequence(model.hidden.map(value => property(value.value))), appends: sequence(model.appends.map(value => property(value.value))) }, capabilities: factsSource.traits, surface: { kind: 'model_surface_facts', columns: sequence(model.columns.map((item: ParsedColumn) => ({ kind: 'model_column' as const, property: property(item.propertyName), column: column(item.name), databaseType: databaseKinds[item.type.kind], type: semanticType(item.semanticType), presence: { kind: 'required' as const }, nullability: nullability(item.nullability.kind), source: span }))), casts: sequence(model.casts.map((cast: ParsedCast) => ({ kind: 'model_cast' as const, property: property(cast.column.value), target: castExpressionType(cast), source: span }))), accessors: sequence(model.accessors.map(accessor => ({ kind: 'model_accessor' as const, property: property(accessor.propertyName.value), method: { kind: 'method_name' as const, value: str(accessor.name.value) }, result: semanticType(accessor.computation.result), source: span }))), relations: sequence(model.relations.map((relation: ParsedRelation) => ({ kind: 'model_relation' as const, name: { kind: 'relation_name' as const, value: str(relation.name.value) }, target: { kind: 'model_name' as const, value: str(relation.targetModel.value) }, relation: relationKinds[relation.type], cardinality: relationCardinality(relation.cardinality), key: relationKey(relation), source: span }))), methods: sequence(factsSource.methods), constants: sequence(factsSource.constants) }, source: span };
    return { kind: 'model_ast', definition, facts, source: span };
}
