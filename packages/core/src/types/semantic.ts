/**
 * semantic.ts
 *
 * Coordinating barrel for Semantic Graph & IR v2 types.
 * Preserves 100% backward compatibility for all upstream and downstream consumers.
 *
 * @module core/types/semantic
 */

export * from './semantic/index';