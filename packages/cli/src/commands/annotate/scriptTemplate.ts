/**
 * scriptTemplate.ts
 *
 * PHP discovery script generator for controller #[Response] annotation.
 * Active Consumer: Orchestrates discovery template generation.
 *
 * @module cli/commands/annotate/scriptTemplate
 */

export { buildPhpDiscoveryScript } from './template/index';
