# Collection / response regression tests

These tests establish the manifest-to-contract boundary before production
implementation changes.

Passing tests are existing capabilities.
Failing `capability:` tests are intentional evidence of a known limitation.

The tests must not be weakened to make an unsupported manifest shape pass.
